using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class Utils
{
    public static void CollectComponentsRecursively<T>(GameObject root, List<T> components)
    {
        if (root == null || components == null)
            return;

        //if root.GetComponent<T>();
    }
}
